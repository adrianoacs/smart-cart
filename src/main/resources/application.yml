server:
  port: 6060

spring:
#  datasource:
#    url: jdbc:h2:/home/trendt000104/test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/smartcart
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/smartcart

# If your Keycloak is configured to use audience (client) checks, you can configure the accepted audience
# and optionally add a JwtAuthenticationConverter to map roles. Example below is commented out and optional.
#
#spring:
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: https://keycloak.example.com/auth/realms/myrealm
#        jwk-set-uri: https://keycloak.example.com/auth/realms/myrealm/protocol/openid-connect/certs
